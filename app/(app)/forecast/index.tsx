import { ShellScreen } from '@/features/app/components/ShellScreen';

export default function ForecastScreen() {
  return (
    <ShellScreen
      title="Forecast & Orders"
      subtitle="AI sales forecasting and ordering"
      phaseLabel="Forecasting and order preparation will be added in a later phase"
      icon="trending-up-outline"
    />
  );
}
