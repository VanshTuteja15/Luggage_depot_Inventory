import { PlaceholderScreen } from '@/components/screens/PlaceholderScreens';

export default function TransferScreen() {
  return (
    <PlaceholderScreen
      title="Transfer Stock"
      subtitle="Move inventory between locations atomically"
      phase="Phase 7"
    />
  );
}
